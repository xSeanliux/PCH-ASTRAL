name: CI
run-name: CI Run
on: [push]
jobs:
  unit-test: 
    name: Unit tests
    steps: 
      make setup; make py-test
  static-type: 
    name: Static typing
    steps: 
      make setup; make py-static
    
